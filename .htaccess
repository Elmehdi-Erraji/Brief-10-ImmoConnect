<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Brief-10-ImmoConnect/

    # Redirect to the index.php file if not a file or directory
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]


</IfModule>